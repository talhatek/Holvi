<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.holvi">

    <application
        android:name=".ui.HolviApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_holvi_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_holvi_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Holvi">

        <activity
            android:name=".ui.authenticationActivity.AuthenticationActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Holvi.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.menu_screen.MenuActivity"
            android:exported="true"
            android:label="@string/title_activity_menu"
            android:theme="@style/Theme.Holvi.NoActionBar" />

    </application>

</manifest>